# dssh 设计规格（Design Spec）

> 本文件记录 dssh 的**界面结构、视觉系统与关键机制**的设计约定，供实现与评审时对齐。
> 代码层面的架构约定见 [`AGENTS.md`](AGENTS.md)，功能与发布流程见 [`README.md`](README.md)，
> 未完成 / 已延后的事项见 [`TODO.md`](TODO.md)。

---

## 1. 产品定位

dssh 是面向开发与运维的**跨平台（Windows / macOS）SSH 管理器**。它把日常远程工作需要的东西收进一个窗口：

- 连接的**管理**（保存、分组、搜索、导入导出）
- 连接的**使用**（终端、SFTP、端口转发、多终端并行）
- 连接的**观察**（主机监控 / 进程 / 服务 / 日志 / 端口）
- 以及一个能读终端、能执行命令的 **AI 运维助手**

设计上偏向**信息密度**与**键盘效率**，接近原生 IDE 的紧凑观感，而不是宽松的营销式界面。

## 2. 设计原则

- **紧凑、原生、克制**：小圆角、细分隔线、有限动效（约 120–150ms 的颜色/透明度过渡），不用卡片阴影堆叠。详见 `.cursor/rules/design.mdc`。
- **语义化 token**：所有颜色、间距、圆角、尺寸都是 `src/theme/global.css` 里的 CSS 变量；组件只引用 token，**不写字面色值**。
- **主机为中心（Host-centric）**：一台已连接的主机是一个**节点**，它的终端、SFTP、转发、主机工具都挂在这个节点下，而不是散落在各个独立面板里。
- **默认安全**：主机密钥 TOFU 校验；凭据只在 Rust 侧解析；破坏性与远程执行动作需显式确认。
- **可发现 + 可加速**：常用操作有可见入口（图标按钮 / 树上的子项），同时全部收进 ⌘K 命令面板供键盘用户直达。

## 3. 布局结构

顶层是一列：**标题栏 → 主行**。主行从左到右四个区域：

```
┌──────────────────────────────────────────────────────────────┐
│ 标题栏（品牌名 · 拖拽区 · 窗口控件）                          │
├────┬──────────────┬────────────────────────────┬─────────────┤
│ 活 │ 左侧栏        │ 主列                        │ 右侧面板     │
│ 动 │              │ ┌────────────────────────┐ │             │
│ 栏 │ · 会话树      │ │ 顶部标签条 + 终端动作   │ │ AI 助手      │
│    │ · S3 配置     │ ├────────────────────────┤ │   或        │
│ 48 │              │ │ 内容区（终端/分屏/SFTP/ │ │ 主机工具     │
│ px │ 220–820px    │ │ 连接管理器 / S3）        │ │ 280–640px   │
│    │              │ └────────────────────────┘ │             │
└────┴──────────────┴────────────────────────────┴─────────────┘
```

- **活动栏**（48px）：顶部为侧栏折叠、会话、连接管理、S3、AI 助手、主机工具；底部为新建本地终端、设置。左侧栏项切换侧栏内容，右侧两项切换右面板（再点一次关闭）。
- **左侧栏**：`sessions` → 活动会话树 + 收藏快捷启动；`s3` → S3 连接配置列表。`connections` 是整页视图，不占侧栏。
- **主列**：顶部标签条（终端 + SFTP 统一，可拖拽重排、中键关闭）+ 右侧终端动作（分屏按钮）；下方是活动标签的内容。
- **右侧面板**：AI 助手 或 主机工具，二选一，宽度可拖拽并持久化。
- **禅模式**：隐藏活动栏、侧栏、标签条，只留终端；Esc 退出。

### 3.1 会话树（签名交互）

一台已连接主机聚合为一个节点（状态点 + 名称 + `user@host:port` + 终端数），展开后：

```
▾ ● aliyun                     1
  │  j3xie@118.31.169.7:22
  ├─ ＋ 新建终端
  ├─ 📁 SFTP
  ├─ ⇄  端口转发
  ├─ ── 分隔 ──
  ├─ ▣ 终端 1              ● ✕
  └─ ⚡ 断开（危险色）
```

本地终端聚合为「本地终端」节点。树下方是**收藏**快捷启动 + 「全部连接 →」入口；完整的连接库在**连接管理**整页视图里，两者职责不重叠。

### 3.2 主要视图

| 视图 | 触发 | 说明 |
| --- | --- | --- |
| 终端 / 分屏 | 会话树、标签条 | 单终端或 2–4 个面板 |
| SFTP | 节点子项 | 按主机打开，整页文件浏览器 |
| 连接管理 | 活动栏 | 搜索 / 排序 / 网格·列表·分组 / 最近 / 导入导出 |
| S3 | 活动栏 | 独立对象存储工作区（自带子标签） |
| 主机工具 | 右面板 | 监控 / 进程 / 服务 / 日志 / 端口 |
| AI 助手 | 右面板 | ReAct Agent 对话 |

## 4. 视觉系统（Violet / Nebula）

主色为**冷紫罗兰**，底色为石墨灰，深色为默认、浅色为可选。

### 4.1 关键 token

| Token | 深色 | 浅色 |
| --- | --- | --- |
| `--accent` | `#7c6ff0` | `#6d5ce0` |
| `--accent-strong` | `#8b7cf6` | `#5b4bd0` |
| `--accent-soft` | `rgba(124,111,240,.14)` | `rgba(109,92,224,.10)` |
| `--bg-app` | `#16161e` | `#f7f7fb` |
| `--bg-sidebar` / `--bg-panel` | `#1d1d28` | `#eff0f5` / `#ffffff` |
| `--bg-elevated` | `#24242f` | `#ffffff` |
| `--bg-selected` | `#2a2740` | `#e4e0fa` |
| `--border-subtle` / `--border-strong` | `#2a2a38` / `#3a3a4a` | `#e2e2ec` / `#cfcfdc` |
| `--text-strong` / `--text-base` / `--text-muted` | `#f2f2f7` / `#e6e6f0` / `#9e9eb0` | `#17171f` / `#2a2a38` / `#6c6c7e` |
| 状态 | `--success #4ade80` · `--warning #fbbf24` · `--danger #f87171` · `--info #7c9cf0` | |

圆角 `2/4/6/8/999`；间距刻度 `4/8/12/16/20/24`；活动栏 48、标签条与标题栏 36。

### 4.2 终端调色板

`terminal/terminalTheme.ts`：背景 `#14141c`、前景 `#e6e6f0`、光标 `#8b7cf6`（紫罗兰）。

**选中色刻意贴近底色**（`#2b2550`，非活动 `#201c38`）：xterm 的 WebGL 渲染器会把单元格背景烘焙进字形纹理，高对比的选中色会让选中文字的抗锯齿明显不同（看起来像字重变了）。调整这两个值前请先复核该影响（见 [`TODO.md`](TODO.md) 第 10 条）。

### 4.3 终端背景

可选壁纸（图片经后端读成 data URL）+ **背景不透明度**（20–100%）。不透明度调低时终端表面变通透，透出壁纸或应用背景；100% 为纯色。启用透明时 xterm 走 `allowTransparency` 路径。

## 5. 关键机制

### 5.1 主机密钥 TOFU

所有 SSH 连接点（终端、单发命令、SFTP、转发）共用 `Arc<HostKeyVerifier>`：

- **首次连接** → 事件 `ssh://hostkey-prompt` → 指纹确认弹窗 → `respond_host_key_prompt`，信任后写入 `known_hosts.json`
- **指纹匹配** → 静默连接
- **指纹变更** → 直接拒绝并告警（`ssh://hostkey-changed`）

> 不允许再出现「无条件接受任意主机密钥」的 handler。

### 5.2 优雅重连

SSH 终端断线后进入 **30 秒宽限窗口**，以指数退避（1s → 15s 封顶）重试；期间状态为 `Reconnecting`，用户可随时取消。正常 `exit`（干净退出）不触发重连。keepalive 15s / 最多 3 次，保证掉线能在 ~45s 内被察觉。

**注意**：重连拿到的是**新的远端 shell**（原 shell 随连接销毁），前端滚屏保留。需要真正的会话保持请在远端用 tmux / mosh。

### 5.3 分屏

扁平模型：同一标签内 2–4 个面板，单一方向（横或纵），比例可拖拽。**新面板继承被拆分面板所在的主机**——从 SSH 面板分屏会再开一个到同一主机的会话，只有本地面板才开本地 shell。

分屏后 `PaneGrid` 会替换 `TerminalWorkspace`，因此**终端级动作必须放在标签条或命令面板**，放进单个面板会在分屏后消失。

### 5.4 命令面板（⌘K）

模糊搜索三类目标：**已保存连接**（直接连）、**已打开标签**（终端 / SFTP，直接跳）、**动作**。动作按上下文出现——分屏、重连、关闭终端只在有活动终端时列出；打开 SFTP / 端口转发只在当前是 SSH 会话时列出。

## 6. 数据与状态边界

| 位置 | 内容 |
| --- | --- |
| SQLite（应用数据目录） | SSH / S3 连接配置、凭据（当前明文，见 TODO）、AI 会话历史 |
| `known_hosts.json` | 已信任的主机密钥指纹 |
| `localStorage` | 每机独立的界面态：最近连接、标签顺序、面板宽度、禅模式、主题、终端外观、更新代理 |

Tauri 载荷统一 **camelCase**；改命令 / 事件时同步 Rust serde DTO 与 TS 类型和服务层。
